## Cloudflared tunnel config template
## Rename to `config.yml` and replace placeholders before use.

tunnel: <TUNNEL-UUID>
credentials-file: C:\Users\<YOUR-USER>\\.cloudflared\<TUNNEL-UUID>.json
ingress:
  - hostname: rps.example.com    # Replace with your hostname (e.g. rps.yourdomain.com)
    service: tcp://localhost:5555 # Our server listens on TCP port 5555
  - service: http_status:404
